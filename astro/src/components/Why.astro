<style>
  .why-us {
    padding: 4rem 1rem; /* Zmniejszono padding dla lepszego dopasowania na mniejszych ekranach */
    text-align: center;
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.3));
    border-radius: 0.75rem;
    color: #fff;
  }

  .why-us h2 {
    font-size: clamp(2rem, 5vw, 2.5rem); /* Responsywna typografia */
    margin-bottom: 1.5rem;
    color: #fcd34d;
  }

  .why-us p {
    font-size: clamp(1rem, 4vw, 1.5rem); /* Responsywne skalowanie tekstu */
    margin-bottom: 2rem;
    max-width: 90%;
    margin-left: auto;
    margin-right: auto;
  }

  .features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); /* Większa elastyczność kolumn */
    gap: 1.5rem; /* Zmniejszono odstępy dla lepszego dopasowania */
    margin-top: 2rem;
  }

  .feature-box {
    background-color: rgba(255, 255, 255, 0.15);
    padding: 1.5rem;
    border-radius: 0.75rem;
    text-align: center;
    transition: transform 0.3s ease;
  }

  .feature-box:hover {
    transform: translateY(-10px);
  }

  .feature-box h3 {
    font-size: clamp(1.25rem, 3vw, 1.75rem); /* Responsywne nagłówki */
    margin-bottom: 0.75rem;
    color: #fcd34d;
  }

  .feature-box p {
    font-size: clamp(0.875rem, 2.5vw, 1.25rem); /* Responsywny tekst */
  }

  .feature-box .icon {
    font-size: clamp(2rem, 5vw, 3rem); /* Responsywna ikona */
    color: #fcd34d;
    margin-bottom: 1rem;
  }

  @media (max-width: 768px) {
    .why-us {
      padding: 3rem 1rem; /* Mniejsze marginesy dla urządzeń mobilnych */
    }

    .features {
      gap: 1rem; /* Zmniejszenie odstępów */
    }
  }
</style>

<div class="why-us">
  <h2>Dlaczego my</h2>
  <p>
    Oferujemy najwyższą jakość usług w branży, dzięki czemu Twoja strona
    osiągnie sukces!
  </p>
  <div class="features">
    <div class="feature-box">
      <i class="fas fa-briefcase icon"></i>
      <h3>Profesjonalizm</h3>
      <p>
        Nasze doświadczenie gwarantuje najwyższą jakość usług i pełne
        zaangażowanie w każdy projekt.
      </p>
    </div>
    <div class="feature-box">
      <i class="fas fa-lightbulb icon"></i>
      <h3>Innowacje</h3>
      <p>
        Stosujemy najnowsze technologie, aby Twoja strona wyróżniała się na tle
        konkurencji.
      </p>
    </div>
    <div class="feature-box">
      <i class="fas fa-chart-line icon"></i>
      <h3>Wyniki</h3>
      <p>
        Pomagamy zwiększyć konwersję i ruch na Twojej stronie, co przekłada się
        na większe zyski.
      </p>
    </div>
  </div>
</div>
